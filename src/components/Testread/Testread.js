import React, { Component, useEffect, useState } from 'react';
import {Table} from 'react-bootstrap';
import axios from 'axios';
import './Testread.css';

function Testread ({parentToChild}) {
 return(
  {parentToChild}
);

}

export default Testread;
